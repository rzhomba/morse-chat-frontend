import React from 'react'
import SettingsOption from '../SettingsOption'
import InfoIcon from '#icons/info.svg'

const HelpOption = () => {
  return (
    <SettingsOption icon={InfoIcon}
                    label="Help"
                    onClick={() => {}}/>
  )
}

export default HelpOption
